# Screenshots / 60-second path

A recorded GIF is optional. The product path is:

1. Open **Projects** → **60s wizard (counter)** (or empty-state **Start with counter.sv**).
2. ChipSutra creates **First project — counter** and imports `knowledge/golden/counter.sv`.
3. Keep module = **Testbench** (Pure SV) → **Generate**.
4. **Simulate** — Verilator PASS is the bar. ChipSutra does not claim UVM sign-off on Verilator.

Place optional PNGs / a GIF in this folder (`landing.png`, `generate.png`, `waveform.png`, `coverage.png`, `wizard.gif`). README already references them.

Do not commit customer RTL or vendor tool logs.
